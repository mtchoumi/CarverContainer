using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CK_iceCol : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {  
        /*Physics2D.IgnoreCollision(CK_PowerUpScript.ball, CK_PowerUpScript.ballClone, CK_PowerUpScript.spring, 
        CK_PowerUpScript.heart, CK_PowerUpScript.spikes);*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
